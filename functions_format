#include "main.h"

/**
 * _printf - main function
 * @str: main
 * Return: Always 0.
 */

int _printf(const char *format, ...)
{
	int i = 0;

	while (str[i] != '\0')
	{
		_putchar(str[i]);
		i++;
	}
	_putchar('\n');
}
